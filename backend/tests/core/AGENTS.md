# Participant 2: core server tests

- Test task persistence, revisions, confirmation, publication snapshots, scoring, input validation and AI fallback here.
- Use temporary SQLite files and mocked OpenAI transport; no paid API calls or real user data.
- Keep production fixes in participant 2's backend modules and report exact executed commands.
